class GeocodingResult {
    public lat: number;
    public lng: number;

    constructor(lat: number, lng: number) {
        this.lat = lat;
        this.lng = lng;
    }
}

export default GeocodingResult;
